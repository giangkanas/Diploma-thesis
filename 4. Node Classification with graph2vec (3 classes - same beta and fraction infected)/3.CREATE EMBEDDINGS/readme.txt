Για κάθε κόμβο βρίσκω τίς ακμές που τον περιλαμβάνουν και δημιουργώ ενα subgraph. Για κάθε subgraph έτρεξα τον graph2vec αλγόριθμο για 5 διαφορετικα embedding dimensions
dimensions = [16,32,64,128,256]
Ετσι για κάθε κόμβο εχω ένα embedding που προέκυψε από τον αλγόριθμο graph2vec